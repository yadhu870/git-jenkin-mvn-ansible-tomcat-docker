tomcat container usefull or maped files (ex. conf and webapps directorys) presend in the given github url
https://github.com/yadhu870/docker-tomcat.git

download and re-configure the path in tomcat.yml file
then specify the path of tomcat.yml in the jenkinsfile